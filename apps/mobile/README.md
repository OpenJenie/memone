# Mobile app placeholder

Reserved for iOS / React Native application code.

## Suggested next step
Create a React Native app directly in this folder, for example:

```bash
npx react-native@latest init ContextEngineerMobile .
```

Then point API requests to `http://localhost:4000/api` in simulator/dev environments.
